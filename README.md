# atm-simulator

A HTTP Server that handles HTTP requests from the Client and simulates the functionality of an ATM.
The database used is a boltDB key value store and contains some sample users to test the functionality.
